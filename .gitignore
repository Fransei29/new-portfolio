/node-modules
/.next
node_modules

# Variables de entorno. .env.local es el que usa Next.js y contiene las claves
# reales — nunca debe commitearse. .env.example sí, porque no tiene secretos.
.env
.env.local
.env.*.local
!.env.example

# Capturas originales de los case studies. Ya estan convertidas a webp en
# public/img/img/<proyecto>-web/, que es lo que sirve el sitio. Se dejan en
# disco por si hay que reprocesarlas, pero no entran al repo: son ~17MB que
# nadie descarga.
public/NewProjects/
